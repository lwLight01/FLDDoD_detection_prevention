[pytest]
minversion = 8.0
addopts =
    -v
    --tb=short
    --strict-markers
testpaths =
    tests
pythonpath =
    src
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require running DB)
    system: System/E2E tests (require full Docker environment)
