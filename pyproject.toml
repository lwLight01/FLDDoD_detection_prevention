[tool.black]
line-length = 100
target-version = ["py310"]
exclude = '''
/(
    \.git
  | \.venv
  | venv
  | notebooks
  | src/dashboard/node_modules
)/
'''

[tool.isort]
profile = "black"
line_length = 100
src_paths = ["src", "tests"]
